<!-- Code generated by tools/docgen from Go doc comments; DO NOT EDIT. Regenerate with `make docs`. -->

# `core`

import path: `example.com/m/core`

Package core は、golden テスト用の package。

契約の段落。日本語どうしの改行は詰め、 os/exec のような英数字との境目には、空白を入れる。特殊文字 \* \_ \` \< \> \& \| \# \$ \~ \[ \] \\ を含む。

## 使い方

```
r := core.New("x")
fmt.Println(r.Label())
```

バッククォートを含むコード:

`````
echo ```` と ``` を含む
`````

## 規則

- alpha-rule: 箇条書きの 1 項目目。続きの行を含む。
- beta-rule・gamma-rule: 2 項目目。

番号付きのリスト:

1. 最初
2. 次

項目の間に空行があるリスト:

- 一つ目

- 二つ目

## 方針

[`Rules`](#Rules) と [`Rules.Label`](#Rules.Label)、[`New`](#New)、[`Level`](#Level) は、この package の doc link。\[Unknown\] と `strings.Builder` は、リンクにならず、コードとして出る。リンクは [example](https://example.com/a?b=c#d) を見る。[https://example.org/auto](https://example.org/auto) も自動でリンクになる。

## API

### <a id="DefaultName"></a>`const DefaultName`

```go
const DefaultName = "default"
```

DefaultName は、既定の名前。

### <a id="Table"></a>`var Table`

```go
var Table = ...
```

Table は、表。値が複数行にわたる初期値は、文書では "..." になる。

### <a id="Version"></a>`var Version`

```go
var Version = "1.0"
```

Version は、1 行の初期値なので、そのまま出る。

### <a id="Helper"></a>`func Helper`

```go
func Helper(a, b int) (sum int, err error)
```

Helper は、関数。

### <a id="Level"></a>`type Level`

```go
type Level int
```

Level は、水準を表す。

#### <a id="Low"></a><a id="High"></a>`const Low, High`

```go
const (
	// Low は、低い水準。
	Low Level = iota
	// High は、高い水準。
	High
)
```

水準の定数。

### <a id="Rules"></a>`type Rules`

```go
type Rules struct {
	Name string // 名前
	// Tags は、タグ。
	Tags []string
	// contains filtered or unexported fields
}
```

Rules は、規則の集まり。

#### <a id="New"></a>`func New`

```go
func New(name string) *Rules
```

New は、Rules を作る。

#### <a id="Rules.Label"></a>`func (Rules) Label`

```go
func (r *Rules) Label() string
```

Label は、ラベルを返す。
